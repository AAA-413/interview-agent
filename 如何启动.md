# 如何启动项目（Windows 11 快速版）

> 适用目录：`D:\work\xiaofuge\111\python`
> 推荐用途：下次新会话快速恢复启动、测试与联调上下文

---

## 一、建议先读哪些文件

如果是新会话，建议按这个顺序恢复上下文：

1. `PROGRESS.md`
2. `docs/knowledge-base-phase4-debugging.md`
3. `docs/pytest-setup-design.md`
4. `README.md`
5. `PLAN.md`

---

## 二、当前项目状态

当前已确认：

- 后端：FastAPI
- 前端：React + Vite
- 基础设施：PostgreSQL + Redis + MinIO（Docker Compose）
- Phase 4（知识库管理模块）：前后端已打通
- pytest 已安装并落地到当前 `.venv`
- 已验证知识库最小化测试：`4 passed`

---

## 三、启动基础设施

在项目根目录打开终端：

```bash
cd d:\work\xiaofuge\111\python
docker compose up -d
```

可选检查：

```bash
docker compose ps
```

包含服务：

- PostgreSQL：5432
- Redis：6379
- MinIO：9000 / 9001

---

## 四、启动后端

在项目根目录执行：

```bash
cd d:\work\xiaofuge\111\python
.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8001
```

启动后访问：

- Swagger 文档：`http://localhost:8001/docs`
- 健康检查：`http://localhost:8001/api/health`

---

## 五、启动前端

在前端目录执行：

```bash
cd d:\work\xiaofuge\111\python\frontend
D:\develop\nodejs\node.exe node_modules\vite\bin\vite.js
```

启动后打开：

- 前端页面：`http://localhost:5173`

说明：

- 当前前端通过 Vite 代理把 `/api` 转发到 `http://localhost:8001`
- 如果终端显示端口变化，以终端实际输出为准

---

## 六、安装开发依赖与 pytest

如果是新环境，先在项目根目录执行：

```bash
cd d:\work\xiaofuge\111\python
.venv\Scripts\python.exe -m pip install -e .[dev]
```

查看 pytest 版本：

```bash
.venv\Scripts\python.exe -m pytest --version
```

当前已验证结果：

```text
pytest 9.0.3
```

---

## 七、运行知识库测试

执行：

```bash
cd d:\work\xiaofuge\111\python
.venv\Scripts\python.exe -m pytest tests/test_knowledge_base_services.py -q
```

当前已验证结果：

```text
4 passed
```

---

## 八、推荐启动顺序

### 1）启动依赖服务

```bash
cd d:\work\xiaofuge\111\python
docker compose up -d
```

### 2）启动后端

```bash
cd d:\work\xiaofuge\111\python
.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8001
```

### 3）启动前端

```bash
cd d:\work\xiaofuge\111\python\frontend
D:\develop\nodejs\node.exe node_modules\vite\bin\vite.js
```

### 4）如需验证测试

```bash
cd d:\work\xiaofuge\111\python
.venv\Scripts\python.exe -m pytest tests/test_knowledge_base_services.py -q
```

---

## 九、常用访问地址

- 前端：`http://localhost:5173`
- 后端 Swagger：`http://localhost:8001/docs`
- 后端健康检查：`http://localhost:8001/api/health`
- MinIO Console：`http://localhost:9001`

---

## 十、常见排查点

### 1. 后端起不来

优先确认是否在项目根目录执行，并且使用的是：

```bash
.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8001
```

不要在错误目录下直接运行，否则可能出现：

```text
ModuleNotFoundError: No module named 'app'
```

### 2. pytest 装不上

如果 `.venv\Scripts\python.exe -m pip install -e .[dev]` 失败，先确认 `pyproject.toml` 已包含：

```toml
[tool.hatch.build.targets.wheel]
packages = ["app"]
```

### 3. async 测试直接失败

如果 pytest 提示：

```text
async def functions are not natively supported.
```

说明异步测试没有按 pytest 方式声明；当前仓库里这个问题已经修复，知识库测试文件已补 `@pytest.mark.asyncio`。

### 4. Windows 中文乱码

如果终端打印中文乱码，优先看：

- pytest 退出码
- `passed/failed` 数量
- 接口 JSON 返回

不要只根据控制台中文显示判断是否失败。

---

## 十一、下次新会话可直接复制的最短命令

```bash
cd d:\work\xiaofuge\111\python
docker compose up -d
.venv\Scripts\python.exe -m pip install -e .[dev]
.venv\Scripts\python.exe -m pytest tests/test_knowledge_base_services.py -q
.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8001
```

新开一个终端再执行前端：

```bash
cd d:\work\xiaofuge\111\python\frontend
D:\develop\nodejs\node.exe node_modules\vite\bin\vite.js
```
